interface:
  display_name: "Code Simplifier"
  short_description: "Safely simplify code with size, quality, and perf gates"
  default_prompt: "Use $code-simplifier to reduce code size and complexity across formatter-aligned lines, bytes, and language-appropriate token/complexity metrics, while preserving behavior, readability, lint/test quality gates, and performance on touched hot paths. Load only the relevant language pack (for example Python, Rust, or Wenyan)."
