interface:
  display_name: "PyTorch Lightning"
  short_description: "PyTorch Lightning engineering and training framework (standalone skill)."
  default_prompt: "Use this skill for the task. First clarify the code structure, training objective, and runtime environment."
